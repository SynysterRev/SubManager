import { PaginatedResponse } from './paginated-responsemodel';

describe('PaginatedResponse', () => {
  it('should create an instance', () => {
    expect(new PaginatedResponse()).toBeTruthy();
  });
});
